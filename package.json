{
  "name": "housepoints",
  "version": "1.0.0",
  "description": "HousePoints — a two-player chore tracker with weekly stakes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check setup.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
